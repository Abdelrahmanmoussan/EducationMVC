@model IdentityText.Models.Teacher

@{
    ViewData["Title"] = "Edit Teacher";
}



<!-- نموذج الإدخال -->
<div class="row">
    <div class="col-md-6">
        <!-- عنوان الصفحة -->
        <div class="chart-header mb-4 rounded-3 shadow-sm d-flex justify-content-center align-items-center text-warning">
            <i class="bi bi-pencil-square me-2"></i> Edit Teacher
        </div>
        <div class="form-container p-4 rounded-3 shadow-sm bg-white">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <input type="hidden" asp-for="TeacherId" />

                <div class="mb-3">
                    <label asp-for="UserId" class="form-label"></label>
                    <input asp-for="UserId" class="form-control rounded-pill" />
                    <span asp-validation-for="UserId" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="TeacherHireDate" class="form-label"></label>
                    <input asp-for="TeacherHireDate" class="form-control rounded-pill" />
                    <span asp-validation-for="TeacherHireDate" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="TeacherDB" class="form-label"></label>
                    <input asp-for="TeacherDB" class="form-control rounded-pill" />
                    <span asp-validation-for="TeacherDB" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="TeacherNetAmount" class="form-label"></label>
                    <input asp-for="TeacherNetAmount" class="form-control rounded-pill" />
                    <span asp-validation-for="TeacherNetAmount" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="TeacherNotes" class="form-label"></label>
                    <input asp-for="TeacherNotes" class="form-control rounded-pill" />
                    <span asp-validation-for="TeacherNotes" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="SubjectId" class="form-label"></label>
                    <select asp-for="SubjectId" class="form-select rounded-pill" asp-items="ViewBag.SubjectId">
                        <option value="">-- اختر المادة --</option>
                    </select>
                </div>

                <div class="d-flex justify-content-between align-items-center mt-4">
                    <button type="submit" class="btn btn-outline-primary px-4 rounded-pill shadow-sm">
                        <i class="bi bi-check-circle me-2"></i> Save
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary rounded-pill">
                        <i class="bi bi-arrow-left-circle me-1"></i> Back to List
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- سكريبتات التحقق -->
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
<!-- تنسيقات CSS -->
<style>
    .chart-header {
        background: linear-gradient(to right, #0d6efd, #6610f2);
        color: white;
        padding: 12px 20px;
        font-size: 1.2rem;
        font-weight: bold;
    }

    .form-label {
        font-weight: 500;
    }

    .form-container {
        background-color: #ffffff;
    }

    .form-control,
    .form-select {
        padding-left: 1rem;
        padding-right: 1rem;
    }

    .row {
        display: flex;
        justify-content: center;
    }
</style>
